{
    "mcpServers": {
        "mongodb": {
            "command": "mongodb-mcp-server",
            "args": ["--uri", "${env:MONGODB_URI}"],
            "env": {
                "MONGODB_URI": "${env:MONGODB_URI}"
            }
        }
    }
}
