schema: 'http://localhost:4000/graphql' # или файл src/schema.gql
documents: 'src/**/*.ts'
generates:
    src/graphql/generated.ts:
        plugins:
            - typescript
            - typescript-operations
