export const optionsCities = {
  fields: ['geometry', 'name', 'formatted_address'],
  types: ['(cities)'],
};

export const optionsFullAdress = {
  fields: ['geometry', 'name', 'formatted_address'],
};
